# Razorpay (public + secret)
RAZORPAY_KEY_ID=rzp_test_your_key_id
RAZORPAY_KEY_SECRET=your_key_secret

# Optional: auth secret (must match Streamlit JWT secret if sharing tokens)
REPOGUARD_JWT_SECRET=repoguard_dev_secret_change_me
